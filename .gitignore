node_modules/
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

.env
.env.local
.env*.local

.DS_Store
*.pem
npm-debug.log*
yarn-error.log*

# Arquivos do acervo (PDF/DOCX) ficam fora do versionamento
storage/

# Skills de agente instaladas por `npx skills add` — ferramenta local,
# não faz parte da aplicação. O lockfile fica versionado para que a
# instalação seja reproduzível.
.agents/
.claude/
